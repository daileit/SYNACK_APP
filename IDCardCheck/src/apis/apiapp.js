import Utils from '../app/Utils';
const PREFIX = 'api/';


async function uploadFace(videoData, idNV, IdGroup = 'dps') {
    // let res = await Utils.post_api(PREFIX + 'rollup/upload', dataBody);
    // return res;
}

export {
    uploadFace
}